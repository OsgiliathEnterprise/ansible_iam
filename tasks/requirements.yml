---

- name: Requirements | import localusers
  ansible.builtin.import_role:
    name: tcharl.ansible_localuser
  become: Yes

- name: Requirements | import orchestration role
  ansible.builtin.import_role:
    name: geerlingguy.postgresql
  become: Yes
